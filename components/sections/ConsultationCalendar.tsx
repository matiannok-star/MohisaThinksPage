import React from 'react';

// This component has been disabled as per user request to resolve loading errors.
const ConsultationCalendar = () => {
  return null;
};

export default ConsultationCalendar;